@font-face {
    font-family: "Gang of three";
    src: url("./../assets/fonts/gang_of_three/gang-of-three.ttf");
}

:root {
    --title-font: "Gang of three";
}

h2.title {
    font-size: 4rem;
    color: white;
    font-family: var(--title-font);
}
div.characters-container {
    display: flex;
    gap: 30px;
}

div.characters-container > div.character {
    display: flex;
    flex-direction: column;
    align-items: center;
    justify-content: center;
    position: relative;
    transition: all 200ms ease;
    filter: grayscale(100%);
    border-radius: 5px;
    overflow: hidden;
}

div.characters-container > div.character:hover {
    cursor: pointer;
    filter: grayscale(0%);
    transform: scale(1.05);
}

div.characters-container > div.character::before {
    content: "";
    position: absolute;
    background: tomato;
    height: 100%;
    width: 100%;
    z-index: -1;
}

div.characters-container > div.character:hover::before {
    height: calc(100% + 20px);
    width: calc(100% + 20px);
}

div.characters-container > div.character > img {
    height: 800px;
}
